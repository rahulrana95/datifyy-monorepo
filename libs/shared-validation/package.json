{
  "name": "@datifyy/shared-validation",
  "version": "1.0.0",
  "description": "Shared validation schemas for Datifyy monorepo",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "yarn exec tsc",
    "dev": "yarn exec tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "yarn exec tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "zod": "^3.22.0"
  },
  "peerDependencies": {
    "@datifyy/shared-types": "1.0.0"
  },
  "files": [
    "dist"
  ]
}
