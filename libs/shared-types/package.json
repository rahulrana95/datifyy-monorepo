{
  "name": "@datifyy/shared-types",
  "version": "1.0.0",
  "description": "Shared TypeScript types and enums for Datifyy monorepo",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "yarn exec tsc",
    "dev": "yarn exec tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "yarn exec tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "files": [
    "dist"
  ]
}
